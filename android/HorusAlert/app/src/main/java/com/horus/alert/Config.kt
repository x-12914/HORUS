package com.horus.alert

/** Fixed app configuration. The server URL is not user-editable. */
object Config {
    // Change here if HORUS ever moves to a new host, then rebuild.
    const val SERVER = "https://horus.157.250.205.174.nip.io"
}
